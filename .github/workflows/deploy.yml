name: Deploy Angular to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:
  schedule:
    # Weekly: rebuild Pages so carousel fallback JSON stays fresh without a code push
    - cron: '0 6 * * 1'

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: angular/package-lock.json

      - name: Unit tests (GitHub highlights + scripts)
        run: npm test

      - name: Install dependencies
        working-directory: angular
        run: npm ci

      - name: Refresh GitHub highlights JSON
        env:
          # Optional: create repo secret GH_PAT with repo read scope for higher API limits.
          # If unset, GITHUB_TOKEN from this workflow is used (enough for public repos).
          GH_PAT: ${{ secrets.GH_PAT }}
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY_OWNER: ${{ github.repository_owner }}
        run: node scripts/refresh-github-highlights.mjs

      - name: Build
        working-directory: angular
        run: npm run build

      - name: Resolve build output
        run: |
          if [ -d "angular/dist/portfolio/browser" ]; then
            echo "PAGES_DIR=angular/dist/portfolio/browser" >> "$GITHUB_ENV"
          else
            echo "PAGES_DIR=angular/dist/portfolio" >> "$GITHUB_ENV"
          fi

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ${{ env.PAGES_DIR }}

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
